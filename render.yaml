services:
  - type: web
    name: pdf2word-api
    runtime: docker
    rootDir: server
    plan: free
    autoDeploy: true
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
